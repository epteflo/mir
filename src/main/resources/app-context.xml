<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx" xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xsi:schemaLocation="
  http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-3.0.xsd
  http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
  http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
  http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd">

    <tx:annotation-driven transaction-manager="transactionManager" />

    <context:annotation-config/>

    <!-- Tell Spring where to scan for components -->
    <context:component-scan base-package="hu.imsi.mir" />

    <!-- Specify the Hibernate properties setting file -->
    <context:property-placeholder
            location="file:///${mir.properties}"/>

    <bean id="dataSource"
          class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${jdbc.driverClassName}" />
        <property name="url" value="${jdbc.url}" />
        <property name="username" value="${jdbc.username}" />
        <property name="password" value="${jdbc.password}" />
    </bean>

    <bean id="sessionFactory"
          class="org.springframework.orm.hibernate3.annotation.AnnotationSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <!--property name="annotatedClasses" value="hu.positech.positionlogger.spring.hibernate.model.HBLNode /-->
        <property name="annotatedClasses">
            <list>
                <value>hu.imsi.mir.spring.hibernate.model.HMuseum</value>
                <value>hu.imsi.mir.spring.hibernate.model.HBeacon</value>
            </list>
        </property>
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${hibernate.dialect}</prop>
                <prop key="hibernate.show_sql">${hibernate.show_sql}</prop>
            </props>
        </property>
    </bean>

    <bean id="transactionManager"
          class="org.springframework.orm.hibernate3.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory" />
    </bean>

    <bean id="mirDao"
          class="hu.imsi.mir.spring.hibernate.dao.MirDaoImpl">
        <property name="sessionFactory" ref="sessionFactory" />
    </bean>

    <bean id="mirService"
          class="hu.imsi.mir.spring.hibernate.service.MirServiceImpl">
        <property name="mirDao" ref="mirDao" />
    </bean>

    <bean id="dbHelper"
          class="hu.imsi.mir.util.DBHelper">
        <property name="mirService" ref="mirService" />
    </bean>

    <bean id="applicationContextProvider" class="hu.imsi.mir.util.ApplicationContextProvider"/>

    <tx:annotation-driven transaction-manager="transactionManager"/>
</beans>